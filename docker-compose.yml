services:
  mock:
    image: stoplight/prism:5
    volumes:
      - ./openapi.yml:/work/openapi.yml:ro   # note: repo uses openapi.yml
    command: ["mock", "-h", "0.0.0.0", "-p", "8080", "/work/openapi.yml"]
    ports:
      - "8080:8080"
